apiVersion: v1
kind: Service
metadata:
  name: student-registration-dr-service
  namespace: student-app
spec:
  type: ClusterIP
  selector:
    app: student-registration-dr
  ports:
    - name: http
      port: 80
      targetPort: 5000
